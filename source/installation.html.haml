---
title: Installation
---

.main-content
  .container
    %h1
      %i.fa.fa-cloud-download
      Installation

    %p
      Using git master is recommended, although it may be slightly unstable at times.

    %table.table.table-condensed
      %tr
        %td(colspan="2")
          %h3
            %i.fa.fa-windows
            Windows

      = package_row 'Windows builds by lachs0r',
                    'https://mpv.srsfckn.biz',
                    :"cloud-download"
      = package_row 'Windows builds by shinchiro',
                    'https://sourceforge.net/projects/mpv-player-windows/files',
                    :"cloud-download"
      = package_row 'Compilation instructions', 'https://github.com/mpv-player/mpv/blob/master/DOCS/compile-windows.md'
      = package_row 'MSYS2 source package', 'https://github.com/Alexpux/MINGW-packages/tree/master/mingw-w64-mpv'

      %tr
        %td(colspan="2")
          %h3
            %i.fa.fa-apple
            OS X

      = package_row 'OS X builds by stolendata', 'https://laboratory.stolendata.net/~djinn/mpv_osx/', :"cloud-download"
      = package_row 'Fink', 'http://pdb.finkproject.org/pdb/package.php/mpv'
      = package_row 'Homebrew', 'https://github.com/Homebrew/homebrew-core/blob/master/Formula/mpv.rb'
      = package_row 'MacPorts', 'https://trac.macports.org/browser/trunk/dports/multimedia/mpv/Portfile'

      %tr
        %td(colspan="2")
          %h3
            %i.fa.fa-terminal
            Source code
          %p
            If you want bleeding edge functionality, link against git versions
            of FFmpeg or if a package for your platform is not available or
            updated, you can build mpv from source code. The helper build
            scripts are recommended for most users since they perform a static
            build of FFmpeg and libass as well as mpv.

      = package_row 'git repository', 'https://github.com/mpv-player/mpv/', :"github-alt"
      = package_row 'Latest stable release tarball', 'https://github.com/mpv-player/mpv/releases/latest', :"github-alt"
      = package_row 'Release changelog', 'https://github.com/mpv-player/mpv/releases', :"github-alt"
      = package_row 'Helper build scripts for *nix systems', 'https://github.com/mpv-player/mpv-build', :"github-alt"

      %tr
        %td(colspan="2")
          %h3
            %i.fa.fa-linux
            Linux packages
          %p
            Distributions usually package outdated, unmaintained, and unsupported versions of mpv. This is especially
            true for popular distros like Debian and Ubuntu. You are recommended to use
            mpv-build or 3rd party packages instead.

      = package_row 'Arch (aur, git package)', 'https://aur.archlinux.org/packages/mpv-git/'
      = package_row 'Arch (aur, mpv-build package)', 'https://aur.archlinux.org/packages/mpv-build-git/'
      = package_row 'Debian multimedia (unofficial)', 'http://www.deb-multimedia.org/dists/testing/main/binary-amd64/package/mpv'
      = package_row 'Ubuntu (PPA)', 'https://launchpad.net/~mc3man/+archive/ubuntu/mpv-tests'
      = package_row 'Ubuntu (PPA, with VapourSynth)', 'https://launchpad.net/%7Edjcj/+archive/ubuntu/vapoursynth'

      %tr
        %td(colspan="2")
          %h3
            Other platforms

      = package_row 'FreeBSD (Freshports)', 'https://www.freshports.org/multimedia/mpv/'
      = package_row 'NetBSD', 'http://cvsweb.netbsd.org/bsdweb.cgi/pkgsrc/multimedia/mpv/'
      = package_row 'OpenBSD', 'http://cvsweb.openbsd.org/cgi-bin/cvsweb/ports/multimedia/mpv/'

    %p
      Do you know of a package that is missing?
      = succeed "." do
        = link_to "Write us about it", "https://github.com/mpv-player/mpv.io/issues/new?title=Package missing"

.next-steps
  .container
    %h2 Installed successfully?
    %hr
    .row
      // .col-xs-4
      //   = link_to getting_started_path, class: "composite" do
      //     %i.fa.fa-book.big-circle
      //     %h3 Getting started guide
      .col-xs-6
        = link_to community_path, class: "composite" do
          %i.fa.fa-comments.big-circle
          %h3 Community
      .col-xs-6
        = link_to bug_reports_path, class: "composite" do
          %i.fa.fa-bug.big-circle
          %h3 Nope (Report a bug)
